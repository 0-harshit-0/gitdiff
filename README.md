# gitdiff

A lightweight `git diff` wrapper in GO. It presents the difference in GUI format for better readability.

## build it
`go build`

## move it
`mv gitdiff ~/.local/bin/`
`chmod +x ~/.local/bin/gitdiff`

## run it
`gitdiff`
